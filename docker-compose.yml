services:
  api:
    build:
      context: .
      dockerfile: SmartInventory.Api/Dockerfile
    ports:
      - "8080:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      ASPNETCORE_URLS: http://+:8080
      ConnectionStrings__SmartInventoryDb: Server=db;Database=SmartInventoryDb;User Id=sa;Password=Fkda7890!;TrustServerCertificate=True;Encrypt=False;MultipleActiveResultSets=True
      Redis__ConnectionString: redis:6379
      Jwt__Key: super_secret_jwt_key_123456789_super_secret_key
      Jwt__Issuer: SmartInventory
      Jwt__Audience: SmartInventoryUsers
      Jwt__ExpiresInMinutes: 60
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_started

  db:
    image: mcr.microsoft.com/mssql/server:2022-latest
    environment:
      SA_PASSWORD: Fkda7890!
      ACCEPT_EULA: "Y"
    ports:
      - "1433:1433"
    healthcheck:
      test: /opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P "Fkda7890!" -C -Q "SELECT 1"
      interval: 10s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"